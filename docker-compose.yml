# Inklusport - servicios locales basicos
services:
  mongo:
    image: mongo:7
    container_name: inklusport-mongo
    ports:
      - "27017:27017"
    volumes:
      - inklusport_mongo_data:/data/db

volumes:
  inklusport_mongo_data:
