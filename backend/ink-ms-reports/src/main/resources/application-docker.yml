spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-reports}:${DB_PORT:3306}/${DB_NAME:reports_db}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}

server:
  port: 3006
